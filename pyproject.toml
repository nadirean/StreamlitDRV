[project]
name = "streamlitdrv"
version = "0.1.0"
description = "Streamlit app for interactive dimensionality reduction and data visualization with support for custom CSV/Excel datasets."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.2.5",
    "pandas>=2.2.3",
    "plotly>=6.0.1",
    "scikit-learn>=1.6.1",
    "setuptools>=80.9.0",
    "streamlit>=1.45.0",
    "umap-learn>=0.5.7",
    "pacmap>=0.7.2",
    "openpyxl>=3.0.0",
    "seaborn>=0.13.2",
]

[project.optional-dependencies]
trimap = [
    "trimap>=1.1.4",
    "annoy>=1.17.3",
]

[dependency-groups]
dev = [
    "ruff>=0.8.0",
]

[tool.ruff.lint]
ignore = [
    "C408",
    "BLE001",
    "S112",
]
